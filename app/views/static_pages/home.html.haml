.center.jumbotron
  - if user_signed_in?
    %h2 you have succesfully signed in
    = link_to "Sign Out", destroy_user_session_path, :method => :delete 
  - else
    %h1 
      Welcome to the Sample App
    %h2
      This is the home page for the 
      <a href="http://www.railstutorial.org/">Ruby on Rails Tutorial</a>
      sample application.
    =link_to "Sign up now!", new_user_registration_path, {:class => "btn btn-lg btn-primary", :method => :get}
    =link_to "Log in!", new_user_session_path ,{:class => "btn btn-lg btn-primary", :method => :get}
= link_to image_tag('rails.png', alt: 'Rails logo'), 'http://rubyonrails.org/' 